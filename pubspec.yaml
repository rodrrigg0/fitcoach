name: fitcoach
description: "FitCoach - Tu asistente personal de fitness con IA"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.0
  go_router: ^14.0.0
  provider: ^6.1.0
  http: ^1.2.0
  shared_preferences: ^2.2.0
  flutter_dotenv: ^5.1.0
  uuid: ^4.3.0
  intl: ^0.19.0
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.0.0
  flutter_svg: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
